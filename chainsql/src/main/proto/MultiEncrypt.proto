option java_outer_classname = "EncryptMsg";

message MultiEncrypt {  
    message HashToken{
		required bytes public_hash = 1;
		required bytes token = 2;
	}
    
    //公钥哈希与Token数组
    repeated HashToken hash_token_pair = 1;  
	
	//字符串的密文
	required bytes cipher = 2;
}